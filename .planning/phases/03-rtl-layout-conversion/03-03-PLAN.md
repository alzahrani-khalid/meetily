---
phase: 03-rtl-layout-conversion
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - frontend/src/components/Sidebar/index.tsx
  - frontend/src/components/ModelSettingsModal.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
  - frontend/src/components/AnalyticsDataModal.tsx
  - frontend/src/components/AISummary/index.tsx
  - frontend/src/components/ChunkProgressDisplay.tsx
  - frontend/src/components/BuiltInModelManager.tsx
  - frontend/src/components/TranscriptRecovery/TranscriptRecovery.tsx
  - frontend/src/components/MeetingDetails/SummaryPanel.tsx
  - frontend/src/components/ImportAudio/ImportAudioDialog.tsx
autonomous: true
requirements: [UI-05, UI-06]
must_haves:
  truths:
    - "User in Arabic mode sees Sidebar, ModelSettingsModal, dropdown-menu, AnalyticsDataModal, AISummary, ChunkProgressDisplay, BuiltInModelManager, TranscriptRecovery, SummaryPanel, and ImportAudioDialog rendering as proper RTL mirrors"
    - "User collapses the sidebar in Arabic mode and the animation slides toward the right edge"
    - "User collapses the sidebar in English mode and the animation slides toward the left edge"
    - "Sidebar collapse/expand chevron icons point in the correct direction for each locale"
    - "All 10 hotspot files pass ESLint with zero no-restricted-syntax errors"
  artifacts:
    - path: "frontend/src/components/Sidebar/index.tsx"
      provides: "RTL-converted sidebar with direction-aware collapse animation"
      contains: "start-0"
    - path: "frontend/src/components/ModelSettingsModal.tsx"
      provides: "RTL-converted model settings modal"
      contains: "ms-"
    - path: "frontend/src/components/ui/dropdown-menu.tsx"
      provides: "RTL-converted dropdown menu primitive"
      contains: "ms-"
  key_links:
    - from: "frontend/src/components/Sidebar/index.tsx"
      to: "useLocale"
      via: "direction detection for translate-x branching"
      pattern: "useLocale|isRTL"
---

<objective>
Convert the 10 highest-hit RTL hotspot files from physical-direction to logical Tailwind classes, plus implement the sidebar collapse animation direction branching (UI-06). These 10 files account for ~51% of all physical-direction hits.

Purpose: Deliver the most visually impactful RTL conversions first (UI-05) and the sidebar animation fix (UI-06).
Output: 10 files with zero physical-direction ESLint violations and correct RTL rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-rtl-layout-conversion/03-CONTEXT.md
@.planning/phases/03-rtl-layout-conversion/03-RESEARCH.md
@.planning/phases/03-rtl-layout-conversion/03-UI-SPEC.md
@.planning/phases/03-rtl-layout-conversion/03-02-SUMMARY.md

<interfaces>
<!-- Conversion mapping table (1:1 mechanical replacement) -->
<!--
  ml-{n} -> ms-{n}     (margin-inline-start)
  mr-{n} -> me-{n}     (margin-inline-end)
  pl-{n} -> ps-{n}     (padding-inline-start)
  pr-{n} -> pe-{n}     (padding-inline-end)
  left-{n} -> start-{n}   (inset-inline-start)
  right-{n} -> end-{n}    (inset-inline-end)
  -left-{n} -> -start-{n} (negative inset-inline-start)
  -right-{n} -> -end-{n}  (negative inset-inline-end)
  text-left -> text-start
  text-right -> text-end
  border-l-{n} -> border-s-{n}
  border-r-{n} -> border-e-{n}
  rounded-l-{n} -> rounded-s-{n}
  rounded-r-{n} -> rounded-e-{n}
  space-x-{n} -> space-x-{n} rtl:space-x-reverse  (ADD companion class)
-->

<!-- Sidebar direction detection (from I18nProvider): -->
<!-- import { useLocale } from "@/providers/I18nProvider"; -->
<!-- const locale = useLocale(); -->
<!-- const isRTL = locale === 'ar'; -->

<!-- UI-06 Sidebar collapse animation contract (from UI-SPEC): -->
<!--
  Expanded LTR: translate-x-0, pinned via start-0
  Collapsed LTR: -translate-x-full (slides off left edge)
  Expanded RTL: translate-x-0, pinned via start-0
  Collapsed RTL: translate-x-full (slides off right edge)
  Collapse button: -end-6 (outside the sidebar edge)
  Collapse icon (expanded): LTR=ChevronLeftCircle, RTL=ChevronRightCircle
  Expand icon (collapsed): LTR=ChevronRightCircle, RTL=ChevronLeftCircle
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Sidebar with direction-aware collapse animation (UI-06)</name>
  <files>frontend/src/components/Sidebar/index.tsx</files>
  <read_first>
    - frontend/src/components/Sidebar/index.tsx
    - frontend/src/providers/I18nProvider.tsx
    - .planning/phases/03-rtl-layout-conversion/03-UI-SPEC.md
  </read_first>
  <action>
Convert all physical-direction Tailwind classes in Sidebar/index.tsx AND implement the direction-aware collapse animation.

**Step 1: Direction detection.** Add at the top of the Sidebar component:
```typescript
import { useLocale } from "@/providers/I18nProvider";
// Inside the component:
const locale = useLocale();
const isRTL = locale === 'ar';
```

**Step 2: Physical-direction class conversions** (apply the mapping table to all ~15 hits):
- Line 664: `fixed top-0 left-0` -> `fixed top-0 start-0`
- Line 668: `absolute -right-6` -> `absolute -end-6`
- All `ml-*` -> `ms-*`, `mr-*` -> `me-*`, `pl-*` -> `ps-*`, `pr-*` -> `pe-*`
- All `border-r` -> `border-e`, `border-l` -> `border-s`
- All `text-left` -> `text-start`, `text-right` -> `text-end`
- All `rounded-l-*` -> `rounded-s-*`, `rounded-r-*` -> `rounded-e-*`
- All `space-x-*` occurrences: ADD `rtl:space-x-reverse` as companion class

**Step 3: Collapse animation (UI-06).** Replace the collapse transform logic:
- Line 669 `style={{ transform: 'translateX(50%)' }}` -> compute based on direction:
  ```typescript
  style={{ transform: isRTL ? 'translateX(-50%)' : 'translateX(50%)' }}
  ```
- The main sidebar panel collapse class: use a dynamic class based on direction:
  ```typescript
  // In the sidebar panel className (around line 679):
  // Replace any fixed translate-x with:
  ${isCollapsed ? (isRTL ? 'translate-x-full' : '-translate-x-full') : 'translate-x-0'}
  ```

**Step 4: Chevron icon direction swap** (lines 671-674):
```typescript
{isCollapsed ? (
  isRTL ? <ChevronLeftCircle className="w-6 h-6" /> : <ChevronRightCircle className="w-6 h-6" />
) : (
  isRTL ? <ChevronRightCircle className="w-6 h-6" /> : <ChevronLeftCircle className="w-6 h-6" />
)}
```
Per CLAUDE.md Rule 5: in RTL, ChevronLeftCircle = "expand from right" (drill-in), ChevronRightCircle = "collapse toward right" (return). So:
- Expanded + RTL: ChevronRightCircle (collapse toward right)
- Collapsed + RTL: ChevronLeftCircle (expand from right)
- Expanded + LTR: ChevronLeftCircle (collapse toward left)
- Collapsed + LTR: ChevronRightCircle (expand from left)

**Step 5:** Add `rtl:space-x-reverse` companion to any `space-x-*` classes in the file.

**Step 6: ESLint exceptions.** If any `translate-x` values are in className strings (not inline styles), add `// eslint-disable-next-line no-restricted-syntax -- translate-x has no logical equivalent; branched on dir` above the line.
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/Coding\ Space/meetily/frontend && npx eslint src/components/Sidebar/index.tsx 2>&1 | grep -c "no-restricted-syntax" | grep "^0$" && grep -c "start-0" src/components/Sidebar/index.tsx && grep -c "isRTL" src/components/Sidebar/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar/index.tsx has zero `no-restricted-syntax` ESLint errors
    - File contains `start-0` (converted from `left-0`)
    - File contains `-end-6` (converted from `-right-6`)
    - File contains `isRTL` variable derived from `useLocale()`
    - File contains direction-branched translate-x logic (both `translate-x-full` and `-translate-x-full` paths)
    - File contains direction-swapped chevron icons (ChevronLeftCircle and ChevronRightCircle swapped based on isRTL)
    - File contains no remaining `ml-`, `mr-`, `pl-`, `pr-`, `text-left`, `text-right`, `border-l-`, `border-r-`, `rounded-l-`, `rounded-r-` classes (unless eslint-disabled with justification)
    - Any `space-x-*` has companion `rtl:space-x-reverse`
  </acceptance_criteria>
  <done>Sidebar is fully RTL-converted with direction-aware collapse animation: slides right in Arabic, left in English, correct chevron icons in both directions</done>
</task>

<task type="auto">
  <name>Task 2: Convert remaining 9 hotspot files</name>
  <files>
    frontend/src/components/ModelSettingsModal.tsx,
    frontend/src/components/ui/dropdown-menu.tsx,
    frontend/src/components/AnalyticsDataModal.tsx,
    frontend/src/components/AISummary/index.tsx,
    frontend/src/components/ChunkProgressDisplay.tsx,
    frontend/src/components/BuiltInModelManager.tsx,
    frontend/src/components/TranscriptRecovery/TranscriptRecovery.tsx,
    frontend/src/components/MeetingDetails/SummaryPanel.tsx,
    frontend/src/components/ImportAudio/ImportAudioDialog.tsx
  </files>
  <read_first>
    - frontend/src/components/ModelSettingsModal.tsx
    - frontend/src/components/ui/dropdown-menu.tsx
    - frontend/src/components/AnalyticsDataModal.tsx
    - frontend/src/components/AISummary/index.tsx
    - frontend/src/components/ChunkProgressDisplay.tsx
    - frontend/src/components/BuiltInModelManager.tsx
    - frontend/src/components/TranscriptRecovery/TranscriptRecovery.tsx
    - frontend/src/components/MeetingDetails/SummaryPanel.tsx
    - frontend/src/components/ImportAudio/ImportAudioDialog.tsx
  </read_first>
  <action>
Apply the 1:1 mechanical conversion to each of the 9 remaining hotspot files. For each file:

1. **Read the file** to identify all physical-direction classes
2. **Apply the mapping table:**
   - `ml-{n}` -> `ms-{n}`, `mr-{n}` -> `me-{n}`
   - `pl-{n}` -> `ps-{n}`, `pr-{n}` -> `pe-{n}`
   - `left-{n}` -> `start-{n}`, `right-{n}` -> `end-{n}`
   - `-left-{n}` -> `-start-{n}`, `-right-{n}` -> `-end-{n}`
   - `text-left` -> `text-start`, `text-right` -> `text-end`
   - `border-l-{n}` -> `border-s-{n}`, `border-r-{n}` -> `border-e-{n}`
   - `rounded-l-{n}` -> `rounded-s-{n}`, `rounded-r-{n}` -> `rounded-e-{n}`
   - `space-x-{n}` -> add `rtl:space-x-reverse` companion class (keep the original)
3. **Convert ALL occurrences** in a single element at once (never mix physical and logical on the same element)
4. **Do NOT change any non-directional classes** (items-center, justify-between, gap-*, etc.)

**Per-file expected hit counts** (from grep analysis):
- ModelSettingsModal.tsx: ~19 hits (14 physical-dir + 2 space-x + 3 left/right positioning)
- dropdown-menu.tsx: ~11 hits (7 physical-dir + 4 left/right positioning)
- AnalyticsDataModal.tsx: ~6 hits
- AISummary/index.tsx: ~11 hits (4 physical-dir + 7 space-x)
- ChunkProgressDisplay.tsx: ~7 hits (1 physical-dir + 6 space-x)
- BuiltInModelManager.tsx: ~6 hits
- TranscriptRecovery.tsx: ~5 hits
- SummaryPanel.tsx: ~5 hits (4 physical-dir + 1 left/right)
- ImportAudioDialog.tsx: ~4 hits

**For dropdown-menu.tsx** (shadcn/ui vendored component): This is safe to edit directly per CONTEXT.md code_context. Apply the same mapping. Pay special attention to `left-*`/`right-*` inset positioning which should become `start-*`/`end-*`.

**For SummaryPanel.tsx:** Check for `left-0` positioning and convert to `start-0`.

After converting each file, run ESLint on it to verify zero violations:
```bash
cd frontend && npx eslint src/components/[FILE] 2>&1 | grep "no-restricted-syntax"
```
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/Coding\ Space/meetily/frontend && npx eslint src/components/ModelSettingsModal.tsx src/components/ui/dropdown-menu.tsx src/components/AnalyticsDataModal.tsx src/components/AISummary/index.tsx src/components/ChunkProgressDisplay.tsx src/components/BuiltInModelManager.tsx src/components/TranscriptRecovery/TranscriptRecovery.tsx src/components/MeetingDetails/SummaryPanel.tsx src/components/ImportAudio/ImportAudioDialog.tsx 2>&1 | grep -c "no-restricted-syntax" | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 files pass ESLint with zero `no-restricted-syntax` errors
    - ModelSettingsModal.tsx contains `ms-` or `me-` (converted from ml-/mr-)
    - dropdown-menu.tsx contains `start-` or `end-` (converted from left-/right- positioning)
    - AISummary/index.tsx contains `rtl:space-x-reverse` (companion for space-x classes)
    - ChunkProgressDisplay.tsx contains `rtl:space-x-reverse`
    - No file contains `ml-`, `mr-`, `pl-`, `pr-`, `text-left`, `text-right`, `border-l-`, `border-r-`, `rounded-l-`, `rounded-r-` in className attributes (unless eslint-disabled with justification)
    - All `space-x-*` occurrences have companion `rtl:space-x-reverse`
  </acceptance_criteria>
  <done>All 9 remaining hotspot files are RTL-converted with zero ESLint violations, accounting for ~51% of all directional hits in the codebase</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan modifies CSS class names only — no new data flows, no new API surfaces, no new authentication.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | T (Tampering) | CSS class conversion | accept | CSS class renaming is presentational only. No security-relevant behavior changes. Visual verification in Phase 6 QA-04. |
</threat_model>

<verification>
1. All 10 hotspot files pass ESLint with zero `no-restricted-syntax` errors
2. Sidebar collapse animation uses direction-branched `translate-x` (isRTL check)
3. Sidebar chevron icons swap correctly based on direction
4. All `space-x-*` usages have `rtl:space-x-reverse` companion
5. No physical-direction classes remain in any of the 10 files (except eslint-disabled exceptions with justification)
</verification>

<success_criteria>
- 10 hotspot files fully converted to logical Tailwind properties
- Sidebar collapse animation slides right in Arabic, left in English (UI-06)
- Chevron icons match the correct direction in both locales
- ESLint zero violations across all 10 files
- ~51% of all directional hits resolved
</success_criteria>

<output>
After completion, create `.planning/phases/03-rtl-layout-conversion/03-03-SUMMARY.md`
</output>
