---
phase: 03-rtl-layout-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/BlockNoteEditor/BlockNoteRTLSpike.tsx
  - .planning/phases/03-rtl-layout-conversion/03-SPIKE-DECISION.md
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "BlockNote RTL spike decision document exists answering all 4 spec section 7 questions"
    - "A test page exists in the repo demonstrating BlockNote with Arabic locale and RTL rendering"
    - "The SUMM-04 editable vs read-only path is locked before any conversion work begins"
  artifacts:
    - path: "frontend/src/components/BlockNoteEditor/BlockNoteRTLSpike.tsx"
      provides: "BlockNote RTL test page with Arabic locale configuration"
      contains: "dictionary"
    - path: ".planning/phases/03-rtl-layout-conversion/03-SPIKE-DECISION.md"
      provides: "Written decision document answering 4 spike questions"
      contains: "Q1:"
  key_links:
    - from: "frontend/src/components/BlockNoteEditor/BlockNoteRTLSpike.tsx"
      to: "@blocknote/core/locales"
      via: "import { ar } from @blocknote/core/locales"
      pattern: "import.*ar.*from.*locales"
---

<objective>
BlockNote RTL spike: time-boxed investigation answering all 4 questions from spec section 7 about BlockNote v0.36.0's RTL capabilities. The outcome locks SUMM-04's path (editable vs read-only rendered markdown) for Phase 5.

Purpose: Per D-01/D-02/D-04, this spike is the FIRST plan in Phase 3 and gates all subsequent work. No conversion work begins until this decision is locked.
Output: A test page (kept in repo), a decision document, and a screenshot — all in the phase directory or component tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rtl-layout-conversion/03-CONTEXT.md
@.planning/phases/03-rtl-layout-conversion/03-RESEARCH.md
@.planning/phases/03-rtl-layout-conversion/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/components/BlockNoteEditor/Editor.tsx (current setup to reference):
```typescript
import { PartialBlock, Block } from "@blocknote/core";
import "@blocknote/shadcn/style.css";
import "@blocknote/core/fonts/inter.css";  // <-- MUST BE REMOVED for spike; Inter lacks Arabic glyphs

const { useCreateBlockNote } = require("@blocknote/react");
const { BlockNoteView } = require("@blocknote/shadcn");

const editor = useCreateBlockNote({
  initialContent: initialContent as PartialBlock[] | undefined,
});
```

From @blocknote/core/locales (verified in node_modules):
```typescript
import { ar } from "@blocknote/core/locales";
// ar contains full Arabic translations: slash menu, toolbar, placeholders, side menu, drag handle
// Pass as: useCreateBlockNote({ dictionary: ar })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BlockNote RTL spike test page</name>
  <files>frontend/src/components/BlockNoteEditor/BlockNoteRTLSpike.tsx</files>
  <read_first>
    - frontend/src/components/BlockNoteEditor/Editor.tsx
    - frontend/src/app/globals.css
  </read_first>
  <action>
Create a new file `frontend/src/components/BlockNoteEditor/BlockNoteRTLSpike.tsx` that tests all 4 spike questions:

1. **RTL text rendering (Q1):** Create a `"use client"` component that renders a BlockNote editor with Arabic content. Set `dir="rtl"` on the wrapping div. Use `style={{ fontFamily: "var(--font-sans-ar)" }}` to inherit Tajawal.

2. **Slash menu RTL (Q2):** The editor must be `editable={true}` so the slash menu can be triggered with `/`.

3. **Dictionary prop localization (Q3):** Import `{ ar } from "@blocknote/core/locales"` and pass `dictionary: ar` to `useCreateBlockNote()`.

4. **Cursor behavior (Q4):** Include multi-line Arabic initial content (at least 3 paragraphs) so cursor navigation can be tested.

Key implementation details:
- Import `"@blocknote/shadcn/style.css"` but do NOT import `"@blocknote/core/fonts/inter.css"` (Inter lacks Arabic glyphs per RESEARCH Pitfall 3)
- Use ES module imports (`import { useCreateBlockNote } from "@blocknote/react"` and `import { BlockNoteView } from "@blocknote/shadcn"`) not `require()` — but if the existing Editor.tsx uses `require()` for SSR reasons, follow that same pattern
- Set `initialContent` to an array of 3+ Arabic paragraph blocks using `PartialBlock[]` format:
  ```typescript
  const initialContent: PartialBlock[] = [
    { type: "heading", content: "اختبار دعم اللغة العربية" },
    { type: "paragraph", content: "هذا نص تجريبي لاختبار عرض النص العربي من اليمين إلى اليسار في محرر BlockNote." },
    { type: "paragraph", content: "اكتب / لفتح قائمة الأوامر وتحقق من ظهور التسميات باللغة العربية." },
    { type: "bulletListItem", content: "عنصر قائمة أول" },
    { type: "bulletListItem", content: "عنصر قائمة ثاني" },
    { type: "paragraph", content: "استخدم مفاتيح الأسهم للتنقل بين الأسطر واختبار سلوك المؤشر." },
  ];
  ```
- Export the component as default: `export default function BlockNoteRTLSpike()`
- Add a heading above the editor: `<h2>BlockNote RTL Spike Test</h2>` with test instructions for each question as HTML comments or visible text
  </action>
  <verify>
    <automated>cd /Users/khalidalzahrani/Desktop/Coding\ Space/meetily/frontend && grep -c "dictionary.*ar\|ar.*dictionary" src/components/BlockNoteEditor/BlockNoteRTLSpike.tsx && grep -c "inter.css" src/components/BlockNoteEditor/BlockNoteRTLSpike.tsx | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - BlockNoteRTLSpike.tsx exists at frontend/src/components/BlockNoteEditor/BlockNoteRTLSpike.tsx
    - File contains `dictionary: ar` or `dictionary: ar,` (grep confirms)
    - File does NOT contain `inter.css` (grep -c returns 0)
    - File contains `dir="rtl"` on wrapping element
    - File contains `"use client"` directive
    - File contains at least 3 Arabic content blocks in initialContent
    - File imports from `@blocknote/core/locales`
  </acceptance_criteria>
  <done>BlockNote RTL spike test page exists with Arabic locale, dictionary prop, RTL direction, Tajawal font inheritance, and multi-line Arabic content for all 4 questions</done>
</task>

<task type="auto">
  <name>Task 2: Run spike tests and write decision document</name>
  <files>.planning/phases/03-rtl-layout-conversion/03-SPIKE-DECISION.md</files>
  <read_first>
    - frontend/src/components/BlockNoteEditor/BlockNoteRTLSpike.tsx
    - .planning/phases/03-rtl-layout-conversion/03-CONTEXT.md
    - .planning/phases/03-rtl-layout-conversion/03-UI-SPEC.md
  </read_first>
  <action>
Test the spike page by temporarily mounting it in a visible route or by adding it to a page component, then verify each question.

**Testing approach:** Either:
(a) Temporarily import and render `BlockNoteRTLSpike` in `frontend/src/app/page.tsx` (revert after testing), OR
(b) Create a temporary test route `frontend/src/app/spike/page.tsx` that renders the component (kept as evidence per D-03), OR
(c) If the dev server cannot be started, perform a static analysis of the BlockNote source code in `node_modules/@blocknote/core/src/` to answer each question based on code inspection.

**For each question, document findings:**

- **Q1 (RTL text rendering):** Does the editor render Arabic text right-aligned with correct line wrapping? Check that ProseMirror's CSS inherits `direction: rtl` from the parent div.
- **Q2 (Slash menu):** Does typing `/` open the slash menu? Are the menu items positioned correctly in RTL?
- **Q3 (Dictionary prop):** Are block labels and slash menu items displayed in Arabic (e.g., "فقرة" for paragraph, "عنوان" for heading)? Verify the `ar` locale from `@blocknote/core/locales` contains translations for: `slash_menu`, `formatting_toolbar`, `side_menu`, `placeholders`, `drag_handle`.
- **Q4 (Cursor behavior):** Do arrow keys move correctly in RTL (right-arrow = backward, left-arrow = forward)?

**Write the decision document** at `.planning/phases/03-rtl-layout-conversion/03-SPIKE-DECISION.md` with this structure:

```markdown
# BlockNote RTL Spike Decision

**Date:** 2026-04-09
**Phase:** 03-rtl-layout-conversion
**Requirement:** SUMM-04 path decision

## Questions & Findings

### Q1: Does BlockNote render RTL text correctly?
**Result:** PASS / FAIL
**Evidence:** [description of what was observed or code analysis]

### Q2: Does the slash menu work in RTL?
**Result:** PASS / FAIL
**Evidence:** [description]

### Q3: Can the dictionary prop localize block labels?
**Result:** PASS / FAIL
**Evidence:** [description, list translated strings found in ar locale]

### Q4: Is cursor behavior correct in RTL?
**Result:** PASS / FAIL
**Evidence:** [description]

## Decision

**Overall:** PASS / FAIL (all 4 must pass per D-02)
**SUMM-04 path:** EDITABLE / READ-ONLY MARKDOWN FALLBACK
**Rationale:** [summary]

## Artifacts
- Test page: `frontend/src/components/BlockNoteEditor/BlockNoteRTLSpike.tsx`
- This decision document: `.planning/phases/03-rtl-layout-conversion/03-SPIKE-DECISION.md`
```

If ANY question fails, the decision is READ-ONLY MARKDOWN FALLBACK per D-02.
  </action>
  <verify>
    <automated>test -f "/Users/khalidalzahrani/Desktop/Coding Space/meetily/.planning/phases/03-rtl-layout-conversion/03-SPIKE-DECISION.md" && grep -c "Q1:\|Q2:\|Q3:\|Q4:" "/Users/khalidalzahrani/Desktop/Coding Space/meetily/.planning/phases/03-rtl-layout-conversion/03-SPIKE-DECISION.md"</automated>
  </verify>
  <acceptance_criteria>
    - 03-SPIKE-DECISION.md exists at .planning/phases/03-rtl-layout-conversion/03-SPIKE-DECISION.md
    - File contains answers for all 4 questions (Q1, Q2, Q3, Q4 — grep confirms 4 matches)
    - File contains either "EDITABLE" or "READ-ONLY MARKDOWN FALLBACK" as the SUMM-04 path decision
    - File contains "PASS" or "FAIL" for each question
    - File contains an "Evidence" line for each question
    - The decision is internally consistent: if any Q is FAIL, overall must be FAIL and path must be READ-ONLY
  </acceptance_criteria>
  <done>Decision document exists answering all 4 spike questions with evidence, SUMM-04 path is locked, and the test page is kept in repo as evidence per D-03</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan creates a test page using an already-installed library (BlockNote 0.36.0) and writes a decision document.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I (Information Disclosure) | BlockNoteRTLSpike.tsx | accept | Test page contains only hardcoded Arabic sample text; no user data, no API calls, no secrets. Low risk. |
</threat_model>

<verification>
1. BlockNoteRTLSpike.tsx exists and contains: `dictionary: ar`, `dir="rtl"`, no `inter.css`, Arabic initial content
2. 03-SPIKE-DECISION.md exists with answers to all 4 questions and a locked SUMM-04 path decision
3. The decision is internally consistent (all PASS = editable, any FAIL = read-only)
</verification>

<success_criteria>
- BlockNote RTL spike test page is committed to the repo as permanent evidence
- All 4 spec section 7 questions are answered with evidence
- SUMM-04 editable vs read-only path is locked in the decision document
- No conversion work has started (this plan is Wave 1, conversion is Wave 2+)
</success_criteria>

<output>
After completion, create `.planning/phases/03-rtl-layout-conversion/03-01-SUMMARY.md`
</output>
